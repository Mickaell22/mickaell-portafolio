---
title: MotoVox
area: fullstack
description: Intercomunicador P2P para moto en tiempo real sobre WiFi local. Flutter + C nativo compilado via FFI para filtro de ruido de motor y viento con RNNoise.
stack: ["Flutter", "Dart", "C", "FFI", "Sockets TCP/UDP", "RNNoise"]
github: "https://github.com/Mickaell22/Proyecto-MotoVox"
demo: ""
image: ""
featured: true
date: "2026-05-05"
role: Proyecto propio
---

## Contexto

Quería comunicarme con mi copiloto en moto sin depender de señal celular ni de Bluetooth de corto alcance. La solución: audio P2P sobre WiFi local, con filtro de ruido de motor y viento en tiempo real.

## Cómo funciona

Conductor y copiloto se conectan por la misma red WiFi (o hotspot). Cada equipo anuncia su sala por **broadcast UDP**, así que el otro la descubre solo; la conexión se cierra escaneando un código QR — sin configurar IPs manualmente. El audio viaja por un **socket TCP crudo** con `tcpNoDelay` activado para que el kernel no acumule paquetes esperando llenar el búfer: en una conversación por voz, medio segundo de retraso ya molesta.

No hay servidor intermediario ni librería de RTC: el transporte es socket puro, que sobre una LAN aislada es más simple y más predecible que negociar ICE/STUN para dos equipos que ya se ven en la misma red.

## El desafío técnico

El ruido de una moto (motor + viento) destruye cualquier comunicación de voz sin filtrar. La solución fue integrar **RNNoise** — una librería de supresión de ruido desarrollada por Mozilla — compilada directamente en C para ARM64 y llamada desde Flutter via **FFI (Foreign Function Interface)**.

Esto significa que el procesamiento de audio corre nativamente en el procesador del teléfono, sin pasar por una API externa, con latencia de milisegundos.

## Stack técnico

- **Flutter + Dart:** UI multiplataforma y gestión de la sesión
- **Sockets TCP crudos:** canal de audio P2P sobre WiFi local, con `tcpNoDelay`
- **Broadcast UDP:** descubrimiento de salas en la red sin registro central
- **C nativo (ARM64):** RNNoise compilado con el NDK para arquitectura ARM
- **FFI:** puente entre Dart y el código C nativo
- **QR:** conexión sin configuración de IPs

## Sistema de licencias

La app incluye un sistema de estados: desbloqueado / prueba / vencida. Diseñado para una eventual distribución.

## Por qué destaca

Integrar una librería C nativa en Flutter via FFI para procesamiento de audio en tiempo real no es algo que se vea en proyectos de estudiantes. Requiere entender la arquitectura ARM, compilación cruzada, gestión de memoria manual y la capa de interoperabilidad de Dart.
